New workstream: Home and Garage rebuild (the 3.0 core screens the quiz lands on). Park current work cleanly, cut feat/home-garage-v3 off the same base as the quiz branch, and note the main fast-forward status. Build behind the existing 3.0 flag so the shipped screens stay untouched.

REFERENCE MOCKUPS ARE THE VISUAL SOURCE OF TRUTH. They are in design/mockups/ (01 through 07 plus dialed.css and README.md). Match them in spacing, type sizes, hierarchy, color and copy. Lift exact values from dialed.css. Where this text and a mockup disagree on how something looks, the mockup wins; this text explains behavior and data.

Scope: Home tab (replaces the current index/Today) and Garage tab (new per-bike structure). Tune and Profile tabs untouched. Ride mode / Start Riding is NOT in this workstream (designs pending): the Start Riding button routes to the existing tune flow for now.

DESIGN SYSTEM: Carbon bg, Panel Dark cards, single Dialed Blue accent for selection and primary only, brand color only as a small edge stripe or eyebrow accent. Barlow Condensed Black Italic for headings ONLY. All numeric values Inter 700, regular width, 26 to 34pt. Muted steel glyphs on rows, lit blue only on the active or expanded row. Row touch targets 48dp minimum (56dp on the setup sheet). No blur anywhere; locked content is crisp rows with a lock glyph.

HOME (mockups 01 and 02), same skeleton in both states.
Above the fold: eyebrow with date and context; Barlow headline; the glance card as hero with the % dialed number as the single biggest element, a one-line caption, a square photo tile for the rider's own bike photo (camera prompt when empty), progress bar, bike/setup/version eyebrow and a one-line values summary; the last-ride recap card; the bottom-docked oversized Start Riding button above the tab bar. No weather, no conditions, nothing the app cannot know.
Below the divider: "Next time, try this" card, present only when the last ride has a logged symptom with no applied adjustment, one deterministic rule-base suggestion; Season goal card (rider-set: ride days, engine hours, a race; dashed "Set" empty state); This season stats row (ride days, motos logged, engine hours) with a maintenance line once hours exist, from a per-bike interval default the rider can edit; Next ride card with a rider-picked date and countdown and a TODAY state, with NO notification of any kind attached (night-before push is removed from the plan entirely); Setup story mini timeline tappable into Garage history; free users see the newest version plus "+N more" locked (code decision 2026-09-04, replaces the v1-plus-ghost-v2 idea).
Day-one state (02): value headline, First Steps card with step 1 pre-checked, glance card ghosted with a dashed border and the meter at the endowed 20% with the reason stated, CTA "Set it on the bike" until the first ride, below-fold modules as honest empty states with dashed borders and promise lines, never fake data.
Use the copy in the mockups verbatim; the small print is written in the app's voice on purpose.

GARAGE (mockups 03 to 07).
Bike list (03): ALWAYS the entry, single-bike riders included (one card plus "Add a bike", tap into the bike page; revised 2026-09-04, the single-bike skip is gone). Card = model name in Barlow, one muted line (year, brand, hours), dialed % large on the right, brand-color left stripe. Dashed "Add a bike" is Pro-gated at the add action; grandfathered accounts untouched.
Bike page (04), top to bottom: brand eyebrow with fork type, model headline, identity card (photo tile, dialed % and bar, category caption), two stat tiles (engine hours with "oil at N"; tires front/rear psi with tap to set, Pro-gated at the tap with trigger `tire_pressure`, decision 2026-09-04), SETUPS list with the running one bordered and badged, dashed "New setup" (Pro), SETUP STORY card with the version graph (two lines by default: the two circuits that changed most; tap opens history), "Coming to your garage" rows shown as "soon" only, never zero bars.
Setup detail, the clicker sheet (05): header with setup name and version, compare and share icons, one convention line. Fork card rows: Spring (air pressure in bar for air forks, coil rate in N/mm for spring forks, driven by bike_models fork type), Compression, Rebound. Shock card rows: Spring rate (N/mm), Race sag (mm), Low speed comp, High speed comp (turns), Rebound. Tires row. One primary button "Refine after ride". Collapsed rows are numbers only: NO stock values, NO delta chips vs stock, NO steppers. Tapping a number expands the row: range bar showing position within the adjuster's total travel only if the model's total click range is known, else no bar; end labels; "What it does" block (static educational copy, write all eight adjusters); "Why N for you" block (personalized from quiz inputs and version history; template text now, wired to ai-explain output later); history line; tap-to-fix on the number. One row expanded at a time.
Setup history (06): chip row selecting circuits for the graph (two lit by default), the graph, a timeline of versions each with name, date, exact change, outcome if logged, Compare on old versions, Restore on the oldest, running badge on the current. Pro-only; free users see it as the locked "already recording" row.
Non-running setup page (07): same sheet, tag row (terrain, rides, lineage), values show deltas against the RUNNING setup (our own data, allowed), primary "Run this setup" (one running setup per bike, switching explicit), secondary Refine.
Free vs Pro per plan section 12: free is one bike, one setup, current values only; second bike, multiple setups, history are Pro, shown as visible-but-locked rows.

DATA (staged migrations, not pushed, same discipline as the quiz branch):
- bike_models: total click range per adjuster (fork comp, fork reb, shock LSC, shock reb, HSC turns range), all nullable; bars render only when non-null. Rear sag window nullable for later. Stock values stay unused in UI.
- bikes: hours (already planned), tire_front_psi, tire_rear_psi, fork_spring_rate, shock_spring_rate, photo storage reference, maintenance interval override.
- New: season_goals (type, target, season year); next_ride_date on the profile.
- Events: home_module_viewed per module, goal_set, next_ride_set, sheet_row_expanded per adjuster, story_opened, run_setup_switched.

Vertical slices, each device-testable: (1) Home established state on real data, (2) Home day-one state, (3) Garage bike page and list, (4) setup sheet with expandable rows, (5) history view and non-running setup page. Flag ambiguities before deciding. Append this spec to the v3 plan as the Home and Garage sections. Short status at the end of each session.
